{
  "name": "root",
  "private": true,
  "engines": {
    "node": ">=8",
    "npm": ">=5"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.2.1"
  }
}
